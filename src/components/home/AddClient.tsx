import { FC } from "react";

const AddClient: FC = () => {
  return (
    <div>
      <button className="bg-green-700 text-white py-2 px-3 rounded-[8px] shadow-2xl ">
        + Add Client
      </button>
    </div>
  );
};

export default AddClient;
